import styled from '@emotion/styled';

const StyledBanner = styled(
  'div',
  {}
)(() => ({
//   background: 'rgb(219,19,236)',
  background: 'linear-gradient(180deg, #db13ec 30%, #FFFFFF 70%)',
  width: '100%',
  color: '#ffffff',
  textAlign: 'center',
  height: '40%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  color: '#000'
}));

export { StyledBanner };
